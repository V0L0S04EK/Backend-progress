{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn rag_api:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  },
  "env": {
    "API_KEY": {
      "required": true
    }
  }
}